interface Colors {
    primary: string
    secondary: string
    black: string
    white: string
}

const colors: Readonly<Colors> = {
    primary: "#fc5c65",
    secondary: "#4ECDC4",
    black: "#000",
    white: "#fff"
}

export default colors;
